import React from 'react';
import { Link as RouterLink } from 'react-router-dom';

const style = {
  color: 'black',
  width: '100%',
  height: '100%',
  display: 'block',
};

export default RouterLink;
